[phases.setup]
nixPkgs = ["python310", "tesseract", "libglvnd", "zlib", "libjpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
